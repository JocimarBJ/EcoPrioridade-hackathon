version: "3.9"

services:

  ecoprioridade:
    build: .

    container_name: ecoprioridade_api

    ports:
      - "8000:8000"

    volumes:
      - .:/app

    restart: always

    command: python -m uvicorn api_server:app --host 0.0.0.0 --port 8000 --reload